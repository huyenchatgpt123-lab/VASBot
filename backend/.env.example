DATABASE_URL=postgresql://vabot:vabot123@db:5432/vabot
SECRET_KEY=your-super-secret-key-change-in-production
OPENAI_API_KEY=sk-your-openai-api-key
# Admin API key (platform.openai.com → Settings → Admin keys) — dùng cho Dashboard khớp hóa đơn
OPENAI_ADMIN_API_KEY=
# Tùy chọn nếu tài khoản thuộc nhiều organization
OPENAI_ORG_ID=
# Chi phí OpenAI được cache DB; tự đồng bộ lúc 00:00 (giờ VN) và qua nút trên Dashboard
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
UPLOAD_DIR=./uploads
FAISS_DIR=./faiss_data
# local = lưu ./uploads (dev). cloudinary = production (Render)
STORAGE_BACKEND=local
CLOUDINARY_CLOUD_NAME=xf5muhwl
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CHUNK_SIZE=900
CHUNK_OVERLAP=180
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_COST_PER_1M=0.02
USD_TO_VND=25000
CHAT_MODEL=gpt-4o-mini
